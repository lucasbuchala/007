#!/usr/bin/env perl6
use v6;
use Yu;

multi MAIN($path, Bool :$lint!) {
    my $program = slurp($path);
    for Yu.linter.lint($program) -> $complaint {
        say "line N, column N: $complaint.message()";
    }
}

# vim: ft=perl6
