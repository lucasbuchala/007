#! /bin/sh

rlwrap perl6 -e '
    use _007;
    my $runtime = Yu.runtime;

    while defined my $program = prompt "> " {
        my $ast = Yu.parser(:$runtime).parse($program);
        $runtime.run($ast);
        CATCH {
            default {
                .say;
            }
        }
    }
'
